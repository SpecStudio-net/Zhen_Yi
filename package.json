{
  "name": "zhen-yi",
  "version": "0.1.0",
  "description": "Personal Yìjīng app",
  "scripts": {
    "dev": "concurrently -k -n vite,server -c cyan,yellow \"vite\" \"nodemon --exec 'node --env-file=.env' server.js\"",
    "server": "node --env-file=.env server.js",
    "build": "vite build",
    "start": "NODE_ENV=production node --env-file=.env server.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.35.0",
    "express": "^4.21.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-markdown": "^10.1.0",
    "remark-breaks": "^4.0.0"
  }
}
